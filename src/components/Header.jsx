import React from 'react';
import styled from 'styled-components';

const Header = () => {
  return (
    <Wrapper>
      <h1 className='mb-5'>Teendőim listája</h1>
    </Wrapper>
  );
};

const Wrapper = styled.section``;

export default Header;
